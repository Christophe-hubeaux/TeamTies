<div class="banner">
    <div class="container">
      <%= render "shared/popup" %>
        <h1>Mon dashboard </h1>
        <div class="hello-flex">
        <h2>👋🏻 </h2> <p>Bonjour <%= current_user.pseudo %> ! </p>
        </div>
        <p>Voici tes informations de jeu :</p>
        <div class="infos-flex">
        <p><strong>Équipe : </strong><%= @department.name %></p>
        <p><strong>Nom de la partie :</strong> <%= @game.name %></p>
        <p><strong>Code de la partie :</strong> <%= @game.code %></p>
        </div>
        <div class="buttons-flex">
            <%= button_to "Editer la partie", edit_organisateur_game_path(@game), class: "btn btn-share" %>
            <button class="btn btn-share" type="button" data-bs-toggle="collapse" data-bs-target="#collapseExample" aria-expanded="false" aria-controls="collapseExample">
              Partager cette partie
            </button>

        </div>
      </p>
      <div class="collapse" id="collapseExample">
        <div class="card card-body">
          <div data-controller="clipboard bounce" class="input-group mb-3">
          <div class="input-group-prepend">
            <button class="btn btn-outline-secondary alive" type="button" data-action="mouseup->clipboard#copy mouseup->bounce#startBounce" data-bounce-target="button">Copier</button>
          </div>
          <input type="text" value="<%= url_for(controller: 'games', action: 'join', code: @game.code, only_path: false) %>" class="form-control bg-transparent border-0" placeholder="" aria-label="" aria-describedby="basic-addon1" data-clipboard-target="input" disabled>
        </div>
        </div>
      </div>

        <div class="performance-container">
        <p><strong> Performances sur les 7 derniers jours : </strong></p>
          <div class="flex-perf">
          <div class="perf-category">
            <div class="perf-icon"><%= image_tag "v242_54.png", class: "avatar" %></div>
            <div class="perf-text">+ 12 points</div>
          </div>

          <div class="perf-category">
            <div class="perf-icon"><%= image_tag "v242_55.png", class: "avatar" %></div>
            <div class="perf-text">+ 2 places au classement individuel</div>
          </div>

          <div class="perf-category">
            <div class="perf-icon"><%= image_tag "v242_56.png", class: "avatar" %></div>
            <div class="perf-text">+ 1 place au classement par équipe</div>
          </div>
          </div>
</div>
  </div>
</div>

        <div class="bottom-dashboard">
        <%= render "shared/ranking" %>
        <% if @next_three_matches %>
          <div class="dashboard-match">
          <h2 class="dashboard-match-title"=>PROCHAINS MATCHS</h2>
          <% @next_three_matches.each do |match, prono| %>
            <%= render "shared/match", match: match, prono: prono %>
          <% end %></div>
        <% end %>
        </div>
