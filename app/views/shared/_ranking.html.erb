  <div class="podium-container">
    <div class="podium-ind">
      <div class="podium-category-ind">
        <h2 class="title-podium-cat">PODIUM INDIVIDUEL</h2>
        <div class="podium-table">
          <table>
            <thead>
              <tr>
                <th scope="col">#</th>
                <th scope="col">PSEUDO</th>
                <th scope="col">EQUIPE</th>
                <th scope="col">POINTS</th>
              </tr>
            </thead>
            <tbody>
              <% if @dashboard_individual_rankings.present? %>
                <% @dashboard_individual_rankings.each_with_index do |a, index| %>
                  <tr>
                    <th scope="row"><%= index + 1 %></th>
                    <td><%= a.respond_to?(:pseudo) ? a.pseudo : a[0] %></td>
                    <td><%= a.team %></td>
                    <td><%= a.total_score %></td>
                  </tr>
                <% end %>
              <% else %>
                <tr>
                  <td colspan="3">Aucune donnée de classement n'est disponible pour le moment.</td>
                </tr>
              <% end %>
            </tbody>
          </table>
        </div>
      </div>
    </div>


    <div class="podium-category-col">
      <h2 class="title-podium-cat">PODIUM COLLECTIF</h2>
      <div class="podium-table">
        <table>
          <thead>
            <tr>
              <th scope="col">#</th>
              <th scope="col">EQUIPE</th>
              <th scope="col">POINTS</th>
            </tr>
          </thead>
          <tbody>
            <% @dashboard_department_rankings.each_with_index do |ranking, index| %>
              <tr>
                <th scope="row"><%= index + 1 %></th>
                <td><%= ranking.team %></td>
                <td><%= ranking.average_score %></td>
              </tr>
            <% end %>
          </tbody>
        </table>
      </div>
    </div>
  </div>
