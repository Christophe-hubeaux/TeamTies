<div class="container">
    <div class="page-title">
        <h1>Mes pronos</h1>
    </div>

    <div class="filter-by-date">
    </div>

    <div class="matchs">
        <div class="date">
            <h3>Vendredi 14 juin</h3>
        </div>
        <div class="day-matchs">
            <% @matchs.each do |match| %>
                <%= render "shared/match", match: match %>            
            <% end %>
        </div>
        <div class="date">
            <h3>Samedi 15 juin</h3>
        </div>
        <div class="day-matchs">
            <% @matchs.each do |match| %>
                <%= render "shared/match", match: match %>            
            <% end %>
        </div>
    </div>

</div>

