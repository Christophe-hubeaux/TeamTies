<div class="container mt-5">
    <div class="page-title mt-5">
        <h1 class="prono-title">Scores à encoder</h1>
    </div>
    <div class="d-flex mt-3">
    <%= link_to "Nouveau match", new_admin_match_path, class: "btn signin-btn" %>
    </div>
    <div class="matches">
                <% @matches.each do |match| %>
                    <%= render "shared/match-result", match: match %>
                <% end %>
    </div>
</div>
